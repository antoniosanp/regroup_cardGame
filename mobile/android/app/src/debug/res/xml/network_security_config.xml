<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: Android 9+ (API 28+) blocks plain HTTP/WS (cleartext) traffic
  by default. The dev backend runs on plain http://, not https://, so
  without this, api_client.dart's POST /api/players and the STOMP WebSocket
  connect would both fail silently against the emulator/localhost. This file
  only applies to debug builds (src/debug/res/xml) — release builds have no
  cleartext exception and must talk to a real HTTPS backend.

  10.0.2.2 is the Android emulator's alias for the host machine's localhost
  (see api_client.dart's backendHttpUrl()). If testing on a physical device
  over Wi-Fi instead, add the host machine's LAN IP here too and pass it via
  the dart-define named BACKEND_URL (see api_client.dart for the exact flag).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
