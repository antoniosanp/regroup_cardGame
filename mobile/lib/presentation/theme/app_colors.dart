import 'package:flutter/material.dart';

/// Wood/parchment palette sampled directly from the web client's
/// styles.css `:root` variables, so the mobile app keeps the same visual
/// identity instead of a generic Material dark theme.
class AppColors {
  static const woodDark = Color(0xFF3F2413);
  static const wood = Color(0xFF7A4A24);
  static const woodLight = Color(0xFFA3703F);
  static const parchment = Color(0xFFECD8A6);
  static const parchmentDark = Color(0xFFD7BD85);
  static const iron = Color(0xFF2B241F);
  static const ironLight = Color(0xFF6B625C);
  static const gold = Color(0xFFD9B64F);
  static const text = Color(0xFF2C1C10);
  static const textLight = Color(0xFFF5EAD0);
  static const muted = Color(0xFF8A7355);
  static const accent = Color(0xFFC94A2F);
  static const good = Color(0xFF3F8F4F);
  static const bad = Color(0xFFB23A3A);
  static const warn = Color(0xFFB8860B);
  static const error = Color(0xFFAA3333);
}
